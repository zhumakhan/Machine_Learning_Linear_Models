# Machine_Learning_Linear_Models
There are implementations of some of well know linear models from sklearn
Perceptron, LinearRegression, LogisticRegression(binary), MClassReg for multi class regression, internally uses LogisticRegression 
